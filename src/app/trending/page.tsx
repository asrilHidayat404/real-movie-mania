import React from "react";

const page = ({ pageName }: { pageName: string }) => {
  switch (pageName) {
    case pageName:
      break;

    default:
      break;
  }
  return <div>page</div>;
};

export default page;
